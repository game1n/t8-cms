export type UserDetailsResponseType = {
    fullName: string;
    id: string;
    createdAt: string;
    phone: string;
}

export type UserDetailsPayloadType = {
    id: string;
    fullName: string;
    phone: string;
}